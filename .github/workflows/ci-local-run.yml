name: WireGuard + Build + Advanced Installer Floating License Check

on:
  push:
    branches:
      - testing

jobs:
  wireguard_example:
    runs-on: windows-latest

    steps:
      # 1) Checkout repository
      - name: Checkout source
        uses: actions/checkout@v4

      # 2) Build .NET solution
      - name: Build project (Release)
        shell: cmd
        run: |
          "C:\Program Files\dotnet\dotnet.exe" build LocalTestRunner.sln -c Release

      - name: Enable Chocolatey Community Source
        shell: powershell
        run: |
          choco source remove -n chocolatey || $true
          choco source add `
            -n chocolatey `
            -s https://community.chocolatey.org/api/v2/ `
            --priority=1
          choco source enable -n chocolatey
          choco source list

      - name: Install Advanced Installer (Latest)
        shell: powershell
        run: |
          choco install advancedinstaller -y --no-progress

      - name: Verify Advanced Installer Installation
        shell: powershell
        run: |
          $aiExe = Get-ChildItem `
            "${env:ProgramFiles(x86)}\Caphyon" `
            -Recurse `
            -Filter "AdvancedInstaller.com" `
            -ErrorAction SilentlyContinue |
            Select-Object -First 1

          if ($null -ne $aiExe) {
              Write-Host " Advanced Installer installed"
              Write-Host "Path: $($aiExe.FullName)"
              & $aiExe.FullName /version
          } else {
              Write-Error " Advanced Installer not found"
              exit 1
          }


      # 5) Install WireGuard
      - name: Install WireGuard
        shell: pwsh
        run: |
          $url = "https://download.wireguard.com/windows-client/wireguard-installer.exe"
          $installerPath = "$env:TEMP\wireguard-installer.exe"
          Invoke-WebRequest -Uri $url -OutFile $installerPath
          Start-Process -FilePath $installerPath -ArgumentList "/S" -Wait -NoNewWindow

          if (!(Test-Path "C:\Program Files\WireGuard\wireguard.exe")) {
            Write-Error "WireGuard installation failed"
            exit 1
          }

      # 6) Write WireGuard configuration file
      - name: Write WireGuard config file
        shell: pwsh
        run: |
          $configContent = @"
          [Interface]
          PrivateKey = ${{ secrets.WIREGUARD_PRIVATE_KEY }}
          Address = 10.0.0.2/24
          DNS = 8.8.8.8, 8.8.4.4

          [Peer]
          PublicKey = gTecIYqs7cW7SyHzS3lwy7MK/Lf9G0MxYg2YhAN90lw=
          AllowedIPs = 0.0.0.0/0
          Endpoint = 114.143.100.202:51820
          PersistentKeepalive = 25
          "@
          $configPath = "$env:TEMP\wg0.conf"
          Set-Content -Path $configPath -Value $configContent -Encoding UTF8

      # 7) Start WireGuard tunnel
      - name: Install and start WireGuard tunnel
        shell: pwsh
        run: |
          $wgExe = "C:\Program Files\WireGuard\wireguard.exe"
          $configPath = "$env:TEMP\wg0.conf"
          Start-Process -FilePath $wgExe -ArgumentList "/installtunnelservice", $configPath -Wait -NoNewWindow
          Start-Sleep -Seconds 12

      # 8) Check floating license server
      - name: Check access to floating license server 172.16.2.20:8080
        shell: pwsh
        run: |
          $aiPath = Join-Path "${{ steps.advinst.outputs.path }}" "bin\advancedinstaller.com"
          Write-Output "Testing floating license connection..."
          & $aiPath /registerfloating 172.16.2.20:8080 -testconnection
          $exitCode = $LASTEXITCODE

          Write-Output "Exit code: $exitCode"
          if ($exitCode -eq 0) {
            Write-Output "SUCCESS: Connection OK + license slot available"
          } else {
            Write-Warning "FAILURE: Code $exitCode (no slot or unreachable)"
          }

          try {
            $tcp = New-Object System.Net.Sockets.TcpClient("172.16.2.20", 8080)
            Write-Output "TCP connect SUCCESS"
            $tcp.Close()
          } catch {
            Write-Warning "TCP connect FAILED: $($_.Exception.Message)"
          }

      # 9) Stop and uninstall WireGuard tunnel (always runs)
      - name: Stop and uninstall WireGuard tunnel
        if: always()
        shell: pwsh
        run: |
          $wgExe = "C:\Program Files\WireGuard\wireguard.exe"
          if (Test-Path $wgExe) {
            & $wgExe /uninstalltunnelservice wg0
            Write-Output "WireGuard tunnel wg0 removed"
          }
